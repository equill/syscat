name: networks_discovered
version: 1

resourcetypes:
# Layer 3
  vrfs:
    notes: VRFs as configured on devices
    attributes:
      description:
    dependent: 'true'

relationships:
# Layer 3
  - uri: /devices/Vrfs/vrfs
    cardinality: 1:many
    dependent: 'true'
